<!DOCTYPE html>
<html>
<head>
    <title>Editar aimento</title>
    <link rel="stylesheet" href="/css/styleModifyFood.css">
</head>
<body>
<header>
    <nav>
        <h2 class="logo"></h2>
        <ul>
            <a href="/listFoods/detailsFood/{{food.id}}?nutritionId={{nutritionId}}"><li>ATRAS</li></a>
        </ul>
    </nav>
</header>
<div class="modify">
    <div class="blackSpace"></div>
    <div class="changes">
        <h1>Editar Food</h1>
        <form action="/listFoods/editFood/{{food.id}}?nutritionId={{nutritionId}}" method="post">
        <input type="hidden" name="_csrf" value="{{token}}"/>
        <label for="name">Nombre:</label>
        <input type="text" id="name" name="name" value="{{food.name}}" required><br><br>
        <label for="type">Tipo:</label>
        <input type="text" id="type" name="type" value="{{food.type}}" required><br><br>
        <label for="calories">Calories:</label>
        <input type="number" id="calories" name="calories" value="{{food.calories}}" required><br><br>
        <input type="submit" value="Editar">
        </form>
    </div>
    <div class="blackSpace"></div>
</div>
</body>
</html>